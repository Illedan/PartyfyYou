<Window x:Class="SpotifyListener.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpotifyListener"
        mc:Ignorable="d"
        xmlns:wpf="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <!--<RowDefinition/>-->
        </Grid.RowDefinitions>
        <TextBox x:Name="YOLOSHIT"/>
        <WebBrowser x:Name="webBrowser" Grid.Row="1" ></WebBrowser>
        <!--<wpf:ChromiumWebBrowser Grid.Row="2" x:Name="webBrowser2"
                                Address="{Binding Text, ElementName=txtBoxAddress}">
        </wpf:ChromiumWebBrowser>-->
    </Grid>
</Window>
